rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Autoriser la lecture et l'écriture pour tout le monde sur tous les documents et collections
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
